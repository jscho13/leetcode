/**
 * @param {number[][]} stones
 * @return {number}
 */
var removeStones = function(stones) {
  // the intuition for this one is very difficult
  // you need to realize that all stones are connected to an island
  // you can count the steps to remove down to one stone

}
